{
  "type": "object",
  "required": ["script", "notes"],
  "additionalProperties": false,
  "properties": {
    "script": {
      "type": "string",
      "description": "Complete locator script (ESM, default export async ({page, context, log}) => tokenId)."
    },
    "notes": {
      "type": "string",
      "description": "One sentence naming the page and element targeted and why."
    }
  }
}
